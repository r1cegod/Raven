# 2026-04-20

## VS Code WSL Cache, Prettier, RTK

- User screenshot showed Python picker still listing broken `.venv` entries from the Windows/UNC `file://wsl.localhost/Ubuntu/home/r1ceg/Raven` workspace identity.
- Verified Prettier extension `esbenp.prettier-vscode` is installed in both Windows VS Code extensions and WSL VS Code server extensions.
- Added VS Code user settings to use Prettier as default formatter with format-on-save for JS/TS/JSON/Markdown/HTML/CSS/SCSS/YAML.
- Removed Raven workspace `python-envs.defaultEnvManager` override that nudged Python env selection toward the system manager.
- Added a zsh compatibility wrapper so `rtk status ...` maps to `rtk git status ...`; verified `rtk status --short --branch` works.
- Rewrote VS Code global storage to point Raven backup/last-active state at `vscode-remote://wsl%2Bubuntu/home/r1ceg/Raven` instead of the UNC identity.
- Stale UNC workspaceStorage cache remains locked while VS Code is open. Do not kill VS Code from Codex; it also terminates the active host. Remove that cache only after the user closes VS Code manually.

## Prettier Project Configuration

- Added repo-level `.prettierrc.json`.
- Added `.prettierignore` for venvs, caches, SQLite/runtime data, scratch, reference code, and daily dev logs.
- Added local VS Code workspace bindings so Prettier owns JS/TS/JSON/Markdown/HTML/CSS/SCSS/YAML, with format-on-save enabled for those languages only.
- Added `.vscode/extensions.json` recommendation for `esbenp.prettier-vscode`.
- Verified JSON validity and `npx --yes prettier --check .prettierrc.json .vscode/settings.json .vscode/extensions.json`.

## GitLens Gray Annotation Cleanup

- User noticed code looked gray-ish around active Python lines.
- Diagnosed the `You, 10 hours...` overlay as GitLens current-line blame/annotation, not Python syntax.
- Disabled noisy GitLens current-line blame, GitLens CodeLens, blame highlighting, and unused-code dimming in both VS Code user settings and Raven workspace settings.
- Verified settings JSON remains valid.

## Python Semantic Color Cleanup

- User clarified the gray-ish tokens were member/property tokens like `get`, `status`, `read`, and `decode`.
- Enabled semantic highlighting explicitly and set method/function tokens to `#DCDCAA`, property/enum member tokens to `#9CDCFE`, and type/class tokens to `#4EC9B0`.
- Applied the semantic token colors in both Raven workspace settings and VS Code user settings.
- Verified settings JSON remains valid.
- Semantic-only coloring did not affect the current file, so added TextMate token overrides for Python function calls, built-in/support functions, member access, and property scopes.

## Python Formatter Consolidation

- User suspected multiple Python formatters were fighting and causing lag.
- Uninstalled/removed active Black Formatter and Python Envs extensions from the VS Code extension host; active extension list now keeps Python, Pylance, Debugpy, Ruff, and Prettier.
- Added `requirements-dev.txt` with `ruff` and installed it into `.venv`.
- Added minimal `ruff.toml`.
- Configured Raven workspace so Python uses Ruff as the only formatter, Python format-on-save is disabled, and Prettier is scoped to non-Python languages.
- Added `.vscode/extensions.json` recommendations for Ruff/Python/Pylance/Prettier and unwanted recommendations for Black Formatter/Python Envs.
- Verified settings JSON, active extension lists, and Ruff availability. Ruff reports current Python files would be reformatted, but they were not auto-formatted.

## Reddit Search Reference Artifact

- Added `docs/reference/reddit_search_clean.py` as a STEAL-mode reference-only artifact.
- The artifact loads Reddit OAuth credentials, requests an app-only token, calls OAuth `/search`, and normalizes post metadata into the same dict keys as the active YouTube search layer.
- Verified `py_compile` passes with the repo `.venv`.
- Live smoke reached the expected credential boundary: missing Reddit credentials returns status `0`, zero candidates, and `missing reddit credentials`.
- Fake-listing normalization smoke passed with one query block and one normalized Reddit candidate.
- Active `src/backend/search/reddit_search.py` remains untouched for Duc to port/build when ready.

## VS Code Performance Optimization

- Researched VS Code/Pylance performance guidance and inspected the live WSL extension host process tree.
- Found the hot path was the remote extension host plus Pylance; stale Teams AI Foundry extension child processes were also running.
- Tuned Raven workspace settings for a lighter editor loop: Pylance light mode, open-files-only diagnostics, indexing off, type checking off, smaller Python analysis surface, watcher/search excludes for venvs/caches/data/logs, Git/GitLens background refresh off, and lower-cost editor UI features.
- Quarantined stale AI Foundry/Windows AI Studio extension folders under `~/.vscode-server/extensions/.disabled-by-codex/` because VS Code CLI reported them uninstalled while their old extension host processes were still spawning.
- Restarted only the Pylance server process, not the VS Code window or extension host. Live `top` sample settled with Pylance around low single-digit CPU and no AI Foundry processes present.

## VS Code Suggestion/Purge Pass

- User noticed function suggestions disappeared after the low-latency profile.
- Cause: Pylance light mode plus disabled indexing/library-code parsing removed too much symbol context.
- Switched Raven workspace to a balanced Python profile: Pylance default mode, indexing on with a cap, library-code parsing on, trigger-character suggestions on, function parentheses on, diagnostics still open-files-only, type checking still off.
- Quarantined unused WSL extensions out of VS Code's load path: extra AI assistants, GitLens/GitHistory, Azure packs, Docker/Azure tooling, Jupyter, Java, Go, Ruby, Clangd, PowerShell, Live Server, and HTML/CSS helper.
- Active WSL extension folder set is now limited to Ruff, Prettier, Python, Pylance, Debugpy, Remote WSL, and ChatGPT/Codex.
- Restarted only Pylance after settings changes. Final `top` sample showed Pylance around `1%` CPU and extension host `0%` CPU after settling.

## VS Code Typing Lag Deep Dive

- User reported raw typing latency around 0.2s per visible update.
- Researched VS Code performance guidance: typing lag should be triaged across renderer/UI process, extension host, and language server rather than blamed on formatters.
- `code --status` showed Raven renderer activity plus a second renderer process in the same VS Code window; WSL extension host and Pylance were also previously spiking.
- Renderer log showed repeated `Extension host (Remote) is unresponsive` events, repeated attempts to auto-update/read `github.copilot-chat`, built-in Copilot auth errors, and a Pylance semantic-token failure.
- Found stale `github.copilot-chat` metadata in both WSL and Windows extension registries after its folder had been quarantined. Cleaned the metadata, disabled Copilot/Copilot Chat workspace settings, disabled extension auto-update/checking for Raven, cleared VS Code `CachedExtensionVSIXs`, and quarantined the restored Copilot Chat folder again.
- Found and removed stray `docs/deep-research-report.md:Zone.Identifier`, a Windows download marker leaked into WSL as a real file.
- Applied a renderer-light editor profile: minimap/sticky scroll/smooth scrolling/smooth caret/bracket pair colorization/occurrence highlighting/semantic highlighting disabled while keeping Pylance completions.
- Final live sample showed Pylance and the WSL extension host at `0%` CPU after settling. The remaining memory in the current extension host requires a window reload to reclaim.

## VS Code No-Delay Speed Profile

- User wanted normal-looking code and no typing delay, and confirmed Copilot is not used.
- Removed Copilot from all active paths: WSL user extension `github.copilot-chat`, WSL built-in `copilot`, Windows user `github.copilot-chat`, Windows built-in `copilot`, and leftover Windows Azure GitHub Copilot folder.
- Cleaned Copilot metadata from both WSL and Windows VS Code extension registries.
- Set Raven workspace `chat.disableAIFeatures=true` and disabled all `github.copilot.*` settings.
- Replaced Pylance with Jedi via `python.languageServer=Jedi`; stopped/removing Pylance from the active extension set.
- Removed Prettier from the active WSL/Windows extension set for the speed profile. Ruff remains the Python formatter/linter path.
- Restored normal-looking code by removing emergency token color overrides and semantic-token customizations; `Dark Modern` owns colors again.
- Active WSL extension set is now only Ruff, Python, Debugpy, Remote WSL, and ChatGPT/Codex. Live sample showed Jedi and the extension host at `0%` CPU after settling.

## VS Code Visual Recovery

- User confirmed typing is smooth but code still looked gray/sad.
- Kept the no-delay architecture: Jedi, Ruff, Python, Debugpy, Remote WSL, ChatGPT/Codex; no Copilot, no Pylance, no Prettier.
- Switched Raven workspace theme to built-in `Monokai` for a more saturated base without installing a theme extension.
- Added cheap TextMate-only Python color rules for functions, classes, parameters, variables/properties, imports/control keywords, and comments.
- Restored bracket pair colorization, active bracket guides, selection highlight, single-file occurrence highlight, line highlight, and match brackets.
- Verified after settling: Jedi at `0%` CPU; WSL extension host near idle.

## Controlled Pylance Restore

- User clarified the real problem is typing latency, not CPU by itself, and wanted normal Pylance feel if the chaotic stack stays removed.
- Restored Pylance from the quarantine on WSL and Windows, and re-registered it in both VS Code extension registries.
- Kept Copilot, Prettier, Black, Python Envs, Azure/Jupyter/Java/etc. removed from active extension paths.
- Switched Raven workspace back to `python.languageServer=Pylance` with latency-aware settings: type checking off, diagnostics open-files-only, auto-import completions off, indexing on but capped at 200 files, semantic highlighting on.
- Removed the invalid/noisy Pylance symlink-follow setting that produced renderer warnings.
- Final sample after warm-up: Pylance near idle, WSL extension host near idle. Old Copilot/Pylance errors remain in historical logs, but no active Copilot/Prettier/Black/Python Envs folders are present.

## Enricher Prompt Production Gate

- Built the active Raven query enricher prompt in `src/backend/data/prompt/enricher.py`.
- User corrected the architecture boundary: the graph must stay one node, use `gpt-5.4-mini`, and let the LLM decide query count.
- Reduced `src/backend/Raven_graph.py` back to the single-node structured-output graph: no post-processing, no dedupe layer, no metadata field, no code-side query-count cap.
- Added the Raven eval runner and dataset: `eval/run_enricher_eval.py` and `eval/enricher_cases.jsonl`.
- Added deterministic contract coverage under `src/backend/test/test_enricher_contract.py`.
- First live eval exposed evaluator brittleness around singular/plural tokens (`lead`/`leads`, `agency`/`agencies`); fixed the evaluator instead of overfitting the prompt.
- Final `gpt-5.4-mini` production gate passed 3 live rounds: 15/15 cases ready across broad lead-gen, B2B cold email, AI agent failure, sales learning, and Reddit complaint targets.
- The LLM chose 5-6 queries per case across the final 15 traces; no eval rule enforced a fixed query count.
- Scope boundary: query enricher seam is production-ready; full Raven loop is not production-ready until enriched queries are wired into search/storage and the rater/evolver gates exist.
