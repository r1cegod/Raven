# 2026-04-19

## Metadata Discovery Workbench

- Added the first end-to-end Raven backend wire:
  `query_enricher -> youtube_search/reddit_search -> sqlite_store -> db`.
- Added shared `SourceCandidate` and `SearchResponse` shapes.
- Added `python3 -m src.backend.discovery_workbench "<query>" --limit 5` as the usable command.
- YouTube client uses the official Data API search endpoint and then fetches video details for statistics.
- Reddit client uses OAuth client-credentials flow and searches public posts through `oauth.reddit.com`.
- SQLite stores one run, one query row per platform, raw API/error response per query, and normalized source candidates.
- Duplicate candidate inserts are skipped at the store boundary; low-level DB inserts now raise real `sqlite3.IntegrityError`.
- Verified Python compile, DB contract script, and discovery runner.

## Current Blocker

- `.env` has `YOUTUBE_API_KEY`, `REDDIT_CLIENT_ID`, and `REDDIT_CLIENT_SECRET` keys, but their values are empty.
- With empty credentials, the runner correctly creates run/query logs with error metadata and inserts zero candidates.

## Demotion To Reference

- User clarified that the active Raven build should be written by Duc, not prebuilt by the agent.
- Moved the generated API/search pipeline modules out of `src/backend/` into `docs/reference/metadata_discovery_workbench/`.
- The parked code is teaching reference only; active implementation continues from `src/backend/db.py`.

## Requirements And Enricher Key

- Added root `requirements.txt` for the active backend imports.
- Wired `src/backend/enricher_graph.py` so `ChatOpenAI` reads `ENRICHER_DEV_KEY` explicitly.
- Installed requirements into `.venv`; `pip check` and a smoke import of the enricher LLM passed.

## Venv Interpreter Repair

- Rebuilt `.venv` with copied Python executables instead of symlinked interpreter files.
- Reinstalled `requirements.txt` into the rebuilt venv.
- Updated local `.vscode/settings.json` to point directly at `/home/r1ceg/Raven/.venv/bin/python`.
- Verified Python 3.12.3, `pip check`, and active backend dependency imports.

## Venv Reinstall Verification

- Recreated `.venv` again with `python3 -m venv --clear --copies .venv`.
- Reinstalled `requirements.txt` into `/home/r1ceg/Raven/.venv/bin/python`.
- Verified the VS Code configured interpreter path exists, is executable, and matches `sys.executable`.
- Confirmed VS Code and Antigravity Python extensions are available from their CLIs.

## VS Code WSL Interpreter Path Fix

- Diagnosed VS Code log path translation: `/home/r1ceg/Raven/.venv/bin/python` was being probed as invalid Windows path `D:\home\r1ceg\Raven\.venv\bin\python`.
- Replaced absolute WSL interpreter settings with workspace-relative paths in `.vscode/settings.json`.
- Added workspace `python.languageServer: Pylance` because logs showed editor support disabled with language server set to None.
- Revalidated settings JSON, venv executable, `pip check`, and dependency imports.

## Windows-Side Python Interpreter Fix

- Confirmed Raven's WSL venv has `.venv/bin/python`, not a Windows `python.exe`.
- Created a separate Windows-side editor venv at `.venv-win/` using Windows Python 3.13.7.
- Installed `requirements.txt` into `.venv-win` and verified `pip check` plus active dependency imports.
- Updated `.vscode/settings.json` to point Windows-side Python tooling at `${workspaceFolder}\.venv-win\Scripts\python.exe`.
- Added `.venv-win/` to `.gitignore`.

## VS Code Remote WSL Setup

- User chose the long-term WSL workflow instead of maintaining a Windows-side editor venv.
- Installed Microsoft VS Code Remote WSL extension `ms-vscode-remote.remote-wsl` locally.
- Launched Raven through `code --remote wsl+Ubuntu /home/r1ceg/Raven`.
- Verified WSL server extensions: Remote WSL, Python, and Pylance under `~/.vscode-server/extensions`.
- Reverted Raven workspace interpreter path to `${workspaceFolder}/.venv/bin/python`.
- Removed temporary `.venv-win`.
- Antigravity WSL server setup still fails from its CLI with a 404 for its own server commit; VS Code Remote WSL is the stable path.

## VS Code Antigravity Layout Migration

- User decided to dump Antigravity and bring the setup/layout into VS Code.
- Migrated useful Antigravity user settings into VS Code while excluding broken Windows PathFinder venv paths and Antigravity-only runtime keys.
- Matched the Antigravity visual state in VS Code settings/storage: dark background `#1f1f1f`, chrome `#181818`, border `#2b2b2b`, left sidebar, 170px sidebar, 525px auxiliary/chat bar, visible auxiliary bar, and 42px activity bar startup state.
- Installed useful extensions across VS Code/WSL: Remote WSL, Python, Pylance, GitLens, Git History, Go, clangd, Docker/Containers, PowerShell, Java pack, and Ruby LSP.
- Verified VS Code WSL server update, Remote WSL launch, JSON settings validity, and Raven `.venv` dependency health.

## Active Backend State

- SQLite foundation exists in `src/backend/db.py`.
- Active YouTube search implementation exists in `src/backend/search/youtube_search.py`.
- Verified with `.venv/bin/python`: `python-dotenv` imports, YouTube search module compiles, and a live neutral `youtube_search(..., max_results=1)` call returned HTTP 200 plus one normalized candidate.
- Generated full-pipeline code remains parked under `docs/reference/metadata_discovery_workbench/` as teaching reference only.
- `docs/reference/youtube_search_list_raw.py` remains the raw `search.list` absorption artifact.

## Next Product Seam

- Commit one active YouTube search result into SQLite:
  search result query log -> candidate rows.
- Do not wire Reddit, rater, crawler, or full pipeline before this seam passes.

## Repo Hygiene Note

- Current untracked root `test/` folder violates the repo rule that backend tests live under backend test folders.
- Do not bless root-level tests; move or recreate the DB contract under the backend test path when cleanup is explicitly requested.
